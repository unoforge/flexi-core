# Flexi Core Package

`packages/core` contains the main logic for Flexi tooling.

- Namespace: `FlexiCore\\`
- Includes commands, services, installers, utilities, and stubs
- Consumed by both `packages/cli` and `packages/laravel`
